# config for the GitVersionTask tool used in build/Mlos.NetCore.Package.props
mode: Mainline
branches:
  master:
    # Remove the "master" branch name from consideration.
    regex: ^main$
    increment: Patch
    tag: ''

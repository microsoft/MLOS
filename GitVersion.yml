# config for the GitVersionTask tool used in build/versions/Mlos.NetCore.Version.props
mode: Mainline
branches:
  master:
    # Remove the "master" branch name from consideration.
    regex: ^main$
    #increment: Patch
    tag: beta
# feature:
#   tag: alpha

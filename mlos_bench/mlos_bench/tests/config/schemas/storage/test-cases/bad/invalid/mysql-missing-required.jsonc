{
    "class": "mlos_bench.storage.sql.storage.SqlStorage",

    "config": {
        "drivername": "mysql",
        "database": "mlos_bench",
        // These two are required:
        //"host": "db.example.com",
        //"username": "username"
    }
}
